# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Data (do not commit raw or processed Lending Club data)
data/processed/
data/llm/
data_source/
*.parquet
*.csv
*.xlsx
!docs/*.xlsx
# Exception: commit the small seed dataset for Colab/LLM training (10k+1k+1k, ~8MB)
!data/llm_seed/
!data/llm_seed/*.jsonl

# LLM adapters are large (multi-GB) — never commit; trained on GCP
artifacts/qwen25_lora/
# Keep XGBoost (~10MB) and logistic (~10KB) so Docker images are self-contained.
# Re-generated by: python -m src.models.train

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local secrets
.env
*.pem
gcp-key*.json
