# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Data (do not commit raw or processed Lending Club data)
data/processed/
data/llm/
data_source/
*.parquet
*.csv
*.xlsx
!docs/*.xlsx
# Exception: commit the small seed dataset for Colab/LLM training (10k+1k+1k, ~8MB)
!data/llm_seed/
!data/llm_seed/*.jsonl

# LLM adapters/base are large — never commit; pulled from Drive at runtime
artifacts/qwen3_lora/
artifacts/qwen3_base/
# Keep XGBoost (~10MB) and logistic (~10KB) so Docker images are self-contained.
# Re-generated by: python -m src.models.train

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local secrets
.env
*.pem
gcp-key*.json
